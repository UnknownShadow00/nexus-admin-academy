import { useEffect, useState } from "react";
import {
  getAdminServiceDeskAssignments, getAdminServiceDeskAttempts, getAdminServiceDeskEvents,
  getAdminServiceDeskHealth, getAdminServiceDeskScenarios, resetAdminServiceDeskAttempt,
  saveAdminServiceDeskAssignment,
} from "../../services/api";

export default function AdminServiceDeskPage() {
  const [scenarios, setScenarios] = useState([]); const [attempts, setAttempts] = useState([]); const [assignments, setAssignments] = useState([]); const [health, setHealth] = useState(null); const [replay, setReplay] = useState(null); const [error, setError] = useState(""); const [studentId, setStudentId] = useState(""); const [scenarioId, setScenarioId] = useState("");
  const load = async () => { try { const [s, a, g, h] = await Promise.all([getAdminServiceDeskScenarios({ suppressToast: true }), getAdminServiceDeskAttempts({ suppressToast: true }), getAdminServiceDeskAssignments({ suppressToast: true }), getAdminServiceDeskHealth({ suppressToast: true })]); setScenarios(s.data); setAttempts(a.data); setAssignments(g.data); setHealth(h.data); } catch (err) { setError(err.userMessage || "Service Desk Lab administration is unavailable."); } };
  useEffect(() => { load(); }, []);
  const inspect = async (id) => setReplay((await getAdminServiceDeskEvents(id, { suppressToast: true })).data);
  const reset = async (id) => { await resetAdminServiceDeskAttempt(id); await load(); await inspect(id); };
  const assign = async (event) => { event.preventDefault(); await saveAdminServiceDeskAssignment({ student_id: Number(studentId), scenario_id: Number(scenarioId), mode: "learning", is_required: true }); setStudentId(""); setScenarioId(""); await load(); };
  if (error) return <main className="mx-auto max-w-5xl p-6"><h1 className="text-2xl font-bold">Service Desk Lab</h1><p className="mt-3">{error}</p></main>;
  return <main className="mx-auto max-w-6xl space-y-6 p-6"><header><p className="text-sm font-semibold text-blue-600">ASSESSMENTS & LABS</p><h1 className="text-3xl font-bold">Service Desk Lab</h1><p className="text-slate-600 dark:text-slate-300">Private-beta scenario review, assignments, and deterministic event replay.</p></header><section className="grid gap-3 md:grid-cols-3"><div className="panel"><p>Published scenarios</p><p className="text-3xl font-bold">{health?.published_count ?? "—"}</p></div><div className="panel"><p>Health</p><p className="text-3xl font-bold">{health?.valid ? "Passing" : "Pending"}</p></div><div className="panel"><p>Assignments</p><p className="text-3xl font-bold">{assignments.length}</p></div></section><section className="panel"><h2 className="text-xl font-bold">Assign scenario</h2><form className="mt-3 flex flex-wrap gap-2" onSubmit={assign}><label className="sr-only" htmlFor="student-id">Student ID</label><input id="student-id" className="input-field max-w-40" required min="1" type="number" value={studentId} onChange={(e) => setStudentId(e.target.value)} placeholder="Student ID" /><label className="sr-only" htmlFor="scenario-id">Scenario ID</label><select id="scenario-id" className="input-field max-w-64" required value={scenarioId} onChange={(e) => setScenarioId(e.target.value)}><option value="">Choose scenario</option>{scenarios.map((item) => <option key={item.id} value={item.id}>{item.title}</option>)}</select><button className="btn-primary" type="submit">Assign learning mode</button></form></section><section className="panel"><h2 className="text-xl font-bold">Scenarios</h2><ul className="mt-3 grid gap-2 md:grid-cols-2">{scenarios.map((item) => <li key={item.id} className="rounded border p-3"><strong>{item.title}</strong><br /><span className="text-sm">{item.stable_key} · {item.status}</span></li>)}</ul></section><section className="panel overflow-x-auto"><h2 className="text-xl font-bold">Attempts</h2><table className="mt-3 w-full min-w-[620px] text-left text-sm"><thead><tr><th>ID</th><th>Student</th><th>Version</th><th>Mode</th><th>Result</th><th>Actions</th></tr></thead><tbody>{attempts.map((item) => <tr className="border-t" key={item.id}><td className="py-2">{item.id}</td><td>{item.student_id}</td><td>{item.scenario_version_id}</td><td>{item.mode}</td><td>{item.status} · {item.score ?? 0}%</td><td><button className="btn-secondary mr-2" onClick={() => inspect(item.id)}>Replay</button>{item.mode === "simulation" && item.status !== "in_progress" ? <button className="btn-secondary" onClick={() => reset(item.id)}>Reset</button> : null}</td></tr>)}</tbody></table></section>{replay ? <section className="panel"><h2 className="text-xl font-bold">Attempt {replay.attempt.id} event replay</h2><p className="mt-2">Score: {replay.grade?.overall_score ?? 0}% · {replay.grade?.passed ? "Passed" : "Not passed"}</p><ol className="mt-3 list-decimal space-y-2 pl-5 text-sm">{replay.events.map((event) => <li key={event.sequence_number}><strong>{event.tool}</strong> · {event.event_type} · {event.success ? "accepted" : "rejected"}</li>)}</ol></section> : null}</main>;
}
